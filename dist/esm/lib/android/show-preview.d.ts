export declare class ShowPreview {
    private title;
    private subTitle;
    private confirmLabel;
    private retryLabel;
    private show;
    constructor(show: boolean, title: string, subTitle: string, confirmLabel: string, retryLabel: string);
}
